import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#EF5350",
        borderRadius: 50,
        margin: 5,
    },
    textContainer: {
        padding: 10,
    },
});
